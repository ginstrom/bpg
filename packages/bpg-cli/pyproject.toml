[project]
name = "bpg-cli"
version = "0.1.0"
description = "CLI package for BPG."
readme = "README.md"
requires-python = ">=3.12"

[build-system]
requires = ["uv_build>=0.9.17,<0.10.0"]
build-backend = "uv_build"
