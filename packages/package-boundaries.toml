[packages.bpg_core]
path = "packages/bpg-core"
owns = "Compiler APIs, schema validation, and framework semantics."
allowed_workspace_imports = []
allowed_legacy_imports = ["bpg"]

[packages.bpg_sdk]
path = "packages/bpg-sdk"
owns = "Author-facing SDK interfaces and provider contracts."
allowed_workspace_imports = []
allowed_legacy_imports = ["bpg"]

[packages.bpg_temporal]
path = "packages/bpg-temporal"
owns = "Temporal runtime integration points."
allowed_workspace_imports = ["bpg_langgraph"]
allowed_legacy_imports = ["bpg"]

[packages.bpg_langgraph]
path = "packages/bpg-langgraph"
owns = "LangGraph-backed runtime execution support."
allowed_workspace_imports = []
allowed_legacy_imports = ["bpg"]

[packages.bpg_cli]
path = "packages/bpg-cli"
owns = "CLI entrypoints and command wiring."
allowed_workspace_imports = ["bpg_sdk"]
allowed_legacy_imports = ["bpg"]

[packages.bpg_nodes_core]
path = "packages/bpg-nodes-core"
owns = "Core data-manipulation and control-flow nodes."
allowed_workspace_imports = ["bpg_sdk"]
allowed_legacy_imports = []

[packages.bpg_nodes_ai]
path = "packages/bpg-nodes-ai"
owns = "AI/LLM nodes (Anthropic, OpenAI, Google, Ollama)."
allowed_workspace_imports = ["bpg_sdk"]
allowed_legacy_imports = []

[packages.bpg_nodes_human]
path = "packages/bpg-nodes-human"
owns = "Human-in-the-loop and agent pipeline nodes."
allowed_workspace_imports = ["bpg_sdk"]
allowed_legacy_imports = []

[packages.bpg_nodes_search]
path = "packages/bpg-nodes-search"
owns = "Vector search, embedding, and web search nodes."
allowed_workspace_imports = ["bpg_sdk"]
allowed_legacy_imports = []

[packages.bpg_nodes_comm]
path = "packages/bpg-nodes-comm"
owns = "Communication and integration nodes (email, HTTP, Kafka, Slack)."
allowed_workspace_imports = ["bpg_sdk"]
allowed_legacy_imports = []
